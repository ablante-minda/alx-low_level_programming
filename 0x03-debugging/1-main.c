#include <stdio.h>

/**
 * main - infinite loop
 * Returm: always 0
 */

int main(void)
{
	int i;

	printf("Infinite loop comming :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o\/\n");

	return (0);
}
